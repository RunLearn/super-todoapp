import {createSlice} from '@reduxjs/toolkit';
import { Interface } from 'readline';

interface Todo {
  title: string
  manager: string
  content: string
  deadline: number
  생성일:
  실제완료일(extends: done일때):
  //TODO


}